# WebSocket support for STOMP connections
# This extends the default Elastic Beanstalk proxy configuration

# Map to handle WebSocket upgrade
map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

# Upstream configuration
upstream springboot {
    server 127.0.0.1:5000;
    keepalive 256;
}

